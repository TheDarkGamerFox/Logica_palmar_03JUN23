#include <stdio.h>

int main (){
/*
  variables en c

  las variables son contenedores para almacenar 
  valores de datos, como numeros y carateres.

  En C hay diferente tipos de variables
  (definidas por diferentes palabras), por
  ejemplo:

  *int - almacena enteros(numeros enteros)
  sin decimales como 123 o -123

  *char - almacenas caracteres individuales,
  como 'a' o 'b', los valores de char estan
  entre comillas simples
*/

/*
declaracion (crear) una variables en C, especifique el
tipo y asignele un valor:

*sintaxis

tiposnombreVariable = valor;

doble tipoes es umo de los de las variables de
C(como 9nt) y nombreVariable es el nombre de la variable (como X miNombre). El signi igual se utiliza para asignar un valor de una variable.
*/

//crea una varable que almacena un numero entero



*/
    return 0;
}
